pipelines:
  branches: # Automated triggers on commits to branches
    master: # -- When committing to master branch
    - step:
        deployment: production
        caches:
          - composer
        script:
          - apt-get update
          - apt-get -qq install git-ftp
          - git ftp push --user rishabh@amourint.com --passwd R1shabh@123 ftp://77.104.185.154/public_html/www/erp.amourint.com/sololuxury_app